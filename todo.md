# Supprimer les tâches réalisées

Rajouter un bouton « delete » dans le composant `TaskList` afin de supprimer toutes les tâches déjà réalisées.
Ecrire le script de la fonction qui va supprimer toutes les tâches notée « completed » (utiliser la méthode "filter" sur la liste des tâches).
